Practice Quiz: Introduction to Puppet




Q: A Puppet agent inspects /etc/conf.d, determines the OS to be Gentoo Linux, then activates the Portage package manager. What is the provider in this scenario?  

A: The Puppet agent  





Q: Which of the following examples show proper Puppet syntax?

A: 

class AutoConfig {
  package { 'Executable':
    ensure => latest,
  }
  file { 'executable.cfg':
    source => 'puppet:///modules/executable/Autoconfig/executable.cfg'
    replace => true,
  }
  service { 'executable.exe':
    enable  => true,
    ensure  => running,
  }
}

[ Note: it uses => ]





Q: What is the benefit of grouping resources into classes when using Puppet?

A: Configuration management is simplified





Q: What defines which provider will be used for a particular resource?

A: Puppet assigns providers based on the resource type and data collected from the system.





Q: In Puppet’s file resource type, which attribute overwrites content that already exists?

A: Replace


